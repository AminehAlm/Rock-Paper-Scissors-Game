# Rock Paper Scissors Game - Requirements
# Created by Amineh Alimohammadi
# Python built-in modules only (no extra installations needed)

# Minimum Python version required
python>=3.8

# Built-in dependencies (no pip install needed)
# random - for computer move generation
# datetime - for saving game results with timestamps